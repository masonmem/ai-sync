# AI-assistant configuration audit — 2026-07-12 (host: navi)

> **⚠️ Premise correction:** `~/code/ai-sync` did not exist before this audit — this directory
> was created solely to hold this report, as instructed. The actual shared-config repo is
> **`~/.ai-config`** (`masonmem/ai-config`), and "ai-sync" is the *tool* that manages it
> (`~/.ai-config/bin/ai-sync`). Section 6 of the scan spec was therefore applied to
> `~/.ai-config`. `~/.agents/` also does not exist (no tool on this machine needs it).

Scanned: all repos under `~/code` (2 levels, incl. `archive/*`), `~/.claude`, `~/.codex`,
`~/.copilot`, `~/.config/opencode`, `~/.ai-config`. Read-only; the only file written is this one.

---

## 1. Inventory

### 1a. Repos under ~/code

| Repo | CLAUDE.md | copilot-instructions.md | AGENTS.md | .claude/ | Skills | .mcp.json | Symlinks |
|---|---|---|---|---|---|---|---|
| **homelab** | 23 L | — | — | settings.local.json, skills/ | `komodo-ops` (229 L) in `.copilot/skills/`; `.claude/skills/komodo-ops/SKILL.md` → symlink | — | ✓ resolves (`../../../.copilot/skills/komodo-ops/SKILL.md`) |
| **lines** | 68 L | 179 L | — | settings.local.json, empty `worktrees/` | — | — | none |
| **network** | 85 L | 90 L | — | settings.local.json | `home-network-helper` (58 L) in `.copilot/skills/` | — | none |
| lg-ultragear-dimming-fix | — | — | — | settings.local.json only | — | — | none |
| vision-utils | — | — | — | settings.local.json only | — | — | none |
| atuin, file-sort, mason.nyc, TrailPrint3D | — | — | — | — | — | — | none |
| archive/* (12 dirs) | — | — | — | — | — | — | none |
| ~/code/.claude (not a repo) | — | — | — | settings.local.json only | — | — | none |

Nowhere in `~/code`: `CLAUDE.local.md`, `AGENTS.md`, `.agents/skills/`, `.github/instructions/`,
`.github/skills/`, `.mcp.json`. (The four `SKILL.md` under `vision-utils/.../.venv/.../site-packages/`
are PyPI-vendored fastapi/typer files, excluded.) Extra surface: **`lines/.cursorrules`** (171 B,
Cursor-specific shadcn note).

### 1b. Global surfaces

| Surface | What it reads | State |
|---|---|---|
| `~/.claude/CLAUDE.md` | → `~/.ai-config/instructions.md` | ✓ resolves |
| `~/.claude/skills`, `bin`, `secrets` | → `~/.ai-config/{skills,bin,secrets}` | ✓ all resolve |
| `~/.claude/settings.json` | rendered real file (base + `hosts/navi` overlay) | ⚠ **DRIFTED** per `ai-sync status` (Claude Code writeback) |
| `~/.claude/rules/context7.md` | real file, 10 L | ⚠ machine-local, NOT managed by ai-config |
| `~/.claude/settings.local.json` | real file (5 allow rules, sonarqube off) | correct layer |
| `~/.copilot/copilot-instructions.md` | → `instructions.md` | ✓ resolves |
| `~/.copilot/{skills,bin,secrets,mcp-config.json,settings.json}` | → ai-config | ✓ all resolve |
| `~/.copilot/{agents,instructions,hooks}/` | empty dirs | inert |
| `~/.codex/AGENTS.md` | **does not exist** | ⚠ Codex reads none of the shared brain |
| `~/.codex/config.toml` | 5 lines (approvals, vision-utils trust, TUI nux) | machine-local, fine |
| `~/.codex/skills/` | only `.system/` built-ins; no link to ai-config | ⚠ unwired |
| `~/.config/opencode/AGENTS.md` + `opencode.jsonc` | → **`~/dotfiles/ollama/.config/opencode/`** (not ai-config) | ✓ resolve, but separate brain |
| `~/.agents/` | absent | no consumer — fine |

### 1c. ~/.ai-config (the de-facto "ai-sync" repo)

Remote `git@github.com:masonmem/ai-config.git`, branch `main`. **Working tree dirty:**
modified `hosts/navi/claude-settings.json`, `mcp/servers.toml`, `secrets/manifest.toml`;
untracked `bin/ynab-mcp-wrapper.sh` and **the entire `skills/context7-mcp/` directory**.

Structure: `instructions.md` (51 L), `mcp/servers.toml` (source of truth) → generated `mcp.json`,
`bin/` (ai-sync 44 KB Python + wrappers + statusline), `secrets/` (14 files, all 0600, manifest
tracked), `skills/` (3), `claude/settings.json` + `copilot/settings.json`, `hosts/{navi,solaris}/`
overlays, `launchd/` plist, `tests/` (pytest ×10), `docs/architecture.md`. Fan-out defined in
`bin/ai-sync:88-103` (Claude + Copilot only — **no Codex, no opencode entries**).

`ai-sync status` (run during audit): all 11 symlinks ✓, unifi + ynab MCP ✓, all 14 secrets ✓ 0600;
single failure = `~/.claude/settings.json DRIFTED`.

---

## 2. Content classification

Legend: **G** = GENERAL (portable), **P** = PERSONAL-ONLY, **PS** = PROJECT-SPECIFIC, **DW** = DEAD WEIGHT.

### ~/.ai-config/instructions.md (51 L) — the shared global layer
| Lines | Section | Class | Notes |
|---|---|---|---|
| 1–13 | Title + design comment | G | Meta: keep terse, conditional guidance → skills. Good. |
| 15–18 | Communication | **G** | Concise, lead with answer, push back. |
| 20–23 | Commits | **G** | Conventional Commits; never `Co-authored-by: Copilot`/`Claude`. **Canonical copy — duplicated in 4 other places (§3.1).** |
| 25–27 | Non-interactive sessions | **G** | |
| 29–43 | Personal notes vault | **P** | `~/notes`, notes MCP, agent-drafts airlock. L31–34 claims vault is "symmetric across all agent surfaces" — see contradiction §3.5. |
| 45–51 | Where things live | **P** | Repo map + skill pointers. Correct altitude. |

### ~/.claude/rules/context7.md (10 L)
Entirely **G** in content (Context7 usage steps) but **DW by placement**: duplicates the enabled
`context7` plugin's skill AND the untracked `skills/context7-mcp/SKILL.md` (§3.4), and it lives
outside ai-config, so it silently applies on navi only.

### homelab/CLAUDE.md (23 L)
| Lines | Section | Class |
|---|---|---|
| 1–3 | Intro (hyperion/solaris/Komodo) | **P** |
| 5–12 | Quick orientation (paths, Caddyfile) | **PS** (within a personal repo) |
| 14–17 | Skill pointers | **PS** |
| 19–23 | Gotchas (LaunchAgent kickstart, QTS `/etc` volatility, empty `[secrets]`) | **P** — but all three restate komodo-ops SKILL content (§3.3): borderline **DW** |

### lines/CLAUDE.md (68 L)
| Lines | Section | Class |
|---|---|---|
| 1–3 | Boilerplate header | **DW** |
| 5–11 | Read-these-first (defers to copilot-instructions as canonical) | **PS** — good pattern |
| 13–18 | Working agreements (1 task = 1 issue = 1 PR, `pr-<issue#>-<desc>`) | **PS** (L15 overlaps copilot-instructions L146) |
| 20–34 | Commands (verified §5.2 — all valid) | **PS** |
| 36–57 | Architecture | **PS** |
| 59–64 | Conventions that bite (Biome tabs/quotes = restates formatter → mild **DW**; the `env -u DATABASE_URL` story = **PS**, keep) |
| 66–68 | Deployment (GHCR, Komodo, hyperion) | **PS/P** |

### lines/.github/copilot-instructions.md (179 L) — canonical for the repo
| Lines | Section | Class |
|---|---|---|
| 1–14 | Header, GitHub coords, overview | **PS** |
| 16–32 | Key files (L25 references `.cursorrules`/Cursor — **DW if Cursor retired**, verify) | **PS** |
| 34–46 | Tech stack | **PS** |
| 48–81 | Architecture patterns | **PS** |
| 82–139 | Code style | **G-leaning** — the most portable block in any repo file (naming, error handling, testing habits); L86–90 formatting restates Biome config = **DW** |
| 140–147 | Git / Conventional Commits + branch naming | **G** L142–145 duplicates global (§3.1); L146 branch rule is **PS** |
| 148–170 | Project structure | **PS** |
| 172–179 | Do-nots (L179 "no unsolicited doc files" = **G**, candidate to promote) | **PS** |

### network/CLAUDE.md (85 L)
| Lines | Section | Class |
|---|---|---|
| 1–3 | Boilerplate header | **DW** |
| 5–9 | What this repo is (docs-only, no build/test/CI) | **PS** |
| 11–18 | Scope boundary → homelab | **P** |
| 20–35 | Two-tier source of truth / unifi MCP (known 404 tools listed) | **P** |
| 37–49 | Firewall design + UCG Ultra hands-off | **P** |
| 50–74 | Doc conventions (change-log block, markers, Mermaid) | **PS** |
| 76–81 | Secrets (`x_passphrase` warning) | **P** |
| 83–85 | Companion-skill mirror instruction ("edit here → mirror there") | **DW** — institutionalizes triple maintenance (§3.2) |

### network/.github/copilot-instructions.md (90 L)
Same topics, same order, reworded: L1–11 **PS/P**, L13–34 **P** (MCP), L36–43 **P** (firewall),
L45–54 **PS** (layout; references "transitional `homelab/` dir to be removed" while CLAUDE.md L17
says it was already removed in `32254c7` — stale, **DW**), L55–79 **PS** (conventions, dup of
CLAUDE.md 50–74), L80–86 **P**, L88–90 **G** commits (dup, and wording drifted — §3.1).

### Skills (classification; full quality report in §4)
- `context7-mcp` (53 L): **G** — only fully portable skill. Untracked in git.
- `dotfiles-helper` (156 L): **P** (machine env) + **DW**: restates commit etiquette twice (Golden rule 3 + "Commit etiquette" section).
- `homelab-helper` (114 L): **P** — correct home for this content.
- `home-network-helper` (58 L): **P/PS**; §4 Commits block = **DW** (dup of global).
- `komodo-ops` (229 L): **P/PS** deep ops layer — correct altitude relative to homelab-helper.

---

## 3. Duplication & contradictions

### 3.1 Commit etiquette — 5 copies, 3 wordings
| Location | Wording |
|---|---|
| `~/.ai-config/instructions.md:22-23` | Conventional Commits; **never** `Co-authored-by: Copilot` **or** `Claude` — **canonical** |
| `skills/dotfiles-helper/SKILL.md` (×2: Golden rule 3 + Commit etiquette section) | same policy restated |
| `network/.copilot/skills/home-network-helper/SKILL.md:44-46` | "Never add a `Co-authored-by: Copilot` trailer" — **drift: omits Claude** |
| `network/.github/copilot-instructions.md:90` | "Do not add a `Co-authored-by: Copilot` trailer" — **drift: omits Claude** |
| `lines/.github/copilot-instructions.md:142-145` | Conventional Commits + types — **drift: no co-author rule at all** |
Global instructions are injected into every Claude/Copilot session, so every repo copy is
redundant for those tools; the drifted copies could be read as *permitting* a Claude trailer.

### 3.2 network repo: same conventions maintained in 3 files
`CLAUDE.md` ↔ `.github/copilot-instructions.md` ↔ `home-network-helper/SKILL.md` are a deliberate
near-parallel mirror (change-log block, MCP-first, firewall don't-fix, gateway hands-off,
passphrase warning), enforced by CLAUDE.md:83-85 ("mirror them there"). Already drifting:
copilot-instructions still calls `homelab/` "transitional, to be removed"; CLAUDE.md says removed
in `32254c7`. Contrast with **homelab's better pattern**: one file, symlinked (`.claude/skills/
komodo-ops/SKILL.md → ../../../.copilot/skills/komodo-ops/SKILL.md`, resolves, byte-identical by construction).

### 3.3 Homelab gotchas — 3 copies
Empty-string `[secrets]` Periphery bug: `homelab-helper`, `komodo-ops`, `homelab/CLAUDE.md:19-23`.
solaris `launchctl kickstart` restart: `komodo-ops` + `homelab/CLAUDE.md`. `homelab/CLAUDE.md`'s
whole Gotchas section is a subset of komodo-ops, which auto-loads in that repo.

### 3.4 Context7 — triple guidance, double MCP registration
(1) `~/.claude/rules/context7.md`; (2) untracked `~/.ai-config/skills/context7-mcp/` (serves
Copilot too); (3) enabled `context7@claude-plugins-official` plugin shipping its own skill + MCP
server. This session shows **both `mcp__context7__*` and `mcp__plugin_context7_context7__*` tool
sets** — the server is registered twice (standalone + plugin). Pick one owner.

### 3.5 opencode is a second brain
`instructions.md:31-34` claims vault behaviour is "symmetric across all agent surfaces," but
opencode's `AGENTS.md`/`opencode.jsonc` come from `~/dotfiles/ollama/.config/opencode/` — none of
the shared instructions/skills reach it, and ai-sync doesn't manage it. Not a broken link;
an architecture inconsistency.

### 3.6 Non-findings
No repo has both `copilot-instructions.md` and root `AGENTS.md` (no `AGENTS.md` exists anywhere).
No CLAUDE.md↔AGENTS.md contradictions possible. No chezmoi-era references survive anywhere.
No global↔project contradiction found (lines/network rules refine, don't conflict with, global).

---

## 4. Skills report

| Skill | name+desc | Name=dir | Trigger vs summary | Body lines | Flags |
|---|---|---|---|---|---|
| context7-mcp (global) | ✓ | ✓ | TRIGGER | 53 | Fully generic; **untracked in git**; duplicates context7 plugin skill + rules file (§3.4) |
| dotfiles-helper (global) | ✓ | ✓ | TRIGGER (strong "use when…" + boundary) | 156 | Commit etiquette ×2 = global dup; "where things live" rows dup homelab-helper |
| homelab-helper (global) | ✓ | ✓ | TRIGGER (explicit hyperion/solaris scope) | 114 | Densest (19 KB, 23-rule list) — `references/` split candidate if it grows |
| home-network-helper (network repo) | ✓ | ✓ | TRIGGER (incl. negative boundary) | 58 | Mirrored by 2 instruction files (§3.2); Commits section dup |
| komodo-ops (homelab repo) | ✓ | ✓ | **weak** — "Use when working inside this repo" is vague; body of desc is a summary | 229 | Longest; dual-exposed via symlink (good); gotcha dups (§3.3) |

- Spec validity: all 5 have `name` + `description`, names match dirs, no extra keys, no body >500 lines, no `references/`/`scripts/`.
- `~/.codex/skills/` contains only vendor `.system/` skills — no custom skills, and no link to the shared tree.
- **Promotion candidates:** none upward from repos — `home-network-helper` and `komodo-ops` are
  correctly repo-scoped deep layers (their portable subset already lives in `homelab-helper`).
  The only "promotion" needed is **`context7-mcp` into git** (commit it) and retiring its two duplicates.
- **Demotion/trim candidates:** commit-etiquette blocks out of dotfiles-helper and home-network-helper (§3.1).

---

## 5. Broken / failed / unread items

### 5.1 Structural
1. `~/code/ai-sync` **did not exist** (created only to hold this report). `~/.agents/` absent (harmless).
2. `~/.claude/settings.json` **DRIFTED** (`ai-sync status`) — runtime writeback vs rendered base+overlay.
3. `~/.ai-config` git **dirty**: 3 modified tracked files + untracked `bin/ynab-mcp-wrapper.sh` +
   untracked `skills/context7-mcp/` — the ynab MCP server and a whole skill exist only on navi right now.
4. Codex **unwired**: reads `~/.codex/AGENTS.md`, which doesn't exist; no skills link; no Codex entries
   in the `ai-sync` fan-out. Codex sessions get zero shared instructions.
5. `~/.claude/rules/context7.md` — real file outside ai-config; navi-only, invisible to sync/git.
6. Files nothing reads / inert: `~/.copilot/{agents,instructions,hooks}/` (empty),
   `lines/.claude/worktrees/` (empty), `lines/.cursorrules` (only if Cursor is retired — verify).
7. All 13 symlinks encountered (11 ai-sync-managed + homelab komodo-ops + opencode pair + notes
   inbox) **resolve**; every instruction link uses a filename its tool actually reads. No dangling links found.

### 5.2 Command checks (read-only forms; from `lines/CLAUDE.md:22-34,63`)
| Command | Result |
|---|---|
| `pnpm --version` | ✓ 10.19.0 — matches `packageManager` pin (L34) |
| `pnpm dev/build/check/test/storybook`, `db:*` | ✓ all scripts exist in package.json (not executed — mutating/long-running) |
| `pnpm check` toolchain (`biome --version`) | ✓ 2.3.12 |
| `pnpm test` toolchain (`vitest --version`) | ✓ 4.0.18 |
| `docker compose config --quiet` (dry-run for L23) | ✓ compose file valid; docker 29.6.1 present |
| single-test path `src/lib/relative-time.test.ts` (L31) | ✓ exists |
| **⚠ shell environment bug** | `pnpm` via zsh in **non-interactive** shells explodes: the lazy-nvm shim recurses on missing `_nvm_lazy_load` until FUNCNEST. Works via `/opt/homebrew/bin/pnpm`. Dotfiles bug — hits every agent/CI-ish shell on navi. Also: dual node installs (nvm v24.15.0 + brew v26.5.0). |
| homelab / network commands | none declared (network states "no build, no test, no lint" — CLAUDE.md:7). komodo-ops ssh/launchctl recipes intentionally **not run** (mutate remote state). |
| `ai-sync status` | ✓ runs; reports the settings.json drift above |

---

## 6. PROPOSED MOVE PLAN (not executed)

Attic convention: every removal = `move to ~/code/ai-sync/attic/2026-07-12/<original-path>`.
(If step 1's option B is chosen, the attic lives at `~/.ai-config/attic/2026-07-12/` instead.)

1. **Decide the ai-sync↔ai-config naming** (blocker for the attic path): either
   (a) create symlink `~/code/ai-sync → ~/.ai-config` so both names work, or
   (b) keep `~/.ai-config` as the only name, move `audit/` there, and delete the
   `~/code/ai-sync` directory created today (after moving this report).
   Recommended: (b) — one name, no phantom repo in `~/code`.
2. **Reconcile settings drift**: run `ai-sync diff`, then `ai-sync promote --to overlay` (or
   `base`) for the intentional parts of the `~/.claude/settings.json` writeback; re-run
   `ai-sync status` until clean.
3. **Commit the dirty ai-config state**: review + commit `hosts/navi/claude-settings.json`,
   `mcp/servers.toml`, `secrets/manifest.toml`, `bin/ynab-mcp-wrapper.sh`, and
   `skills/context7-mcp/` (this makes context7-mcp + ynab wiring reach solaris).
4. **De-duplicate Context7** (after 3): keep exactly one owner. Recommended: keep the
   `skills/context7-mcp` skill in ai-config (works for Claude *and* Copilot) + one MCP
   registration in `mcp/servers.toml`; then disable the `context7` plugin in
   `claude/settings.json`, and move `~/.claude/rules/context7.md` → attic. If instead the
   plugin wins, move both the rules file and `skills/context7-mcp/` → attic.
5. **Single-source commit etiquette**: in `skills/dotfiles-helper/SKILL.md` delete the
   "Commit etiquette" section and shrink Golden rule 3 to "see global Commits rules";
   in `network/.copilot/skills/home-network-helper/SKILL.md:44-46` and
   `network/.github/copilot-instructions.md:88-90` replace with one line: "Commits: global
   Conventional-Commits + no co-author-trailer rules apply; most changes here are `docs:`."
   In `lines/.github/copilot-instructions.md:140-147` keep only the repo-specific parts
   (issue refs, branch naming `pr-<issue#>-<desc>`); drop L142-143.
6. **Collapse network's triple mirror to the homelab pattern**: merge the CLAUDE-only deltas
   (known-404 MCP tools list, `32254c7` note) into `.github/copilot-instructions.md`, fix its
   stale "transitional homelab/" line, then replace `network/CLAUDE.md` with a symlink →
   `.github/copilot-instructions.md` (move old CLAUDE.md → attic). Delete the mirror
   instruction (old CLAUDE.md:83-85); the skill stays as the condensed operational layer.
7. **Slim homelab/CLAUDE.md**: drop the Gotchas section (lines 19-23, restates auto-loaded
   komodo-ops) — keep orientation + skill pointers only. Move removed text → attic note.
8. **Wire Codex into the brain**: add to `bin/ai-sync` fan-out (`CODEX_LINKS`):
   `instructions.md → ~/.codex/AGENTS.md` and `skills → ~/.codex/skills/ai-config` (or per-skill
   links beside `.system/`); add pytest coverage in `tests/`; run `ai-sync apply`.
9. **Fold opencode into the fan-out or document the exception**: either move
   `dotfiles/ollama/.config/opencode/AGENTS.md` content into ai-config (an opencode-rendered
   file that appends `instructions.md`), or amend `instructions.md:29-43` to note opencode's
   instructions live in dotfiles. Recommended: fold in, so one brain edits all surfaces.
10. **lines: verify Cursor**: if Cursor is no longer used, move `lines/.cursorrules` → attic and
    delete the `.cursorrules` row from `copilot-instructions.md:25`; if it is used, leave both.
11. **Trim classified DEAD WEIGHT**: boilerplate headers `lines/CLAUDE.md:1-3` and (via step 6's
    merge) `network/CLAUDE.md:1-3`; Biome-restating lines `lines/copilot-instructions.md:86-90`
    and `lines/CLAUDE.md:60` (formatter enforces these). Promote `lines/copilot-instructions.md:179`
    ("no unsolicited doc-summary files") into `~/.ai-config/instructions.md` Communication section.
12. **Sharpen the komodo-ops trigger**: rewrite its description to name concrete triggers
    ("Use when adding/changing a stack, rotating Periphery keys, debugging Komodo/Periphery auth,
    restoring FerretDB backups…") instead of "when working inside this repo".
13. **Fix the navi pnpm shim** (dotfiles repo): repair or guard the `_nvm_lazy_load` zsh wrapper so
    non-interactive shells fall through to `/opt/homebrew/bin/pnpm`; decide between nvm node 24
    and brew node 26 (two installs today).
14. **Housekeeping**: remove empty `lines/.claude/worktrees/`; leave `~/.copilot/{agents,
    instructions,hooks}` (tool-created) unless Copilot CLI tolerates their removal.

Suggested order: 1 → 2 → 3 (unblock git) → 4–7 (dedup) → 8–9 (coverage) → 10–14 (polish).
