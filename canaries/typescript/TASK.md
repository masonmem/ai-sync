# Strict TypeScript canary

Fix the strict TypeScript error in `total.ts` without weakening types, using
`any`, suppressing diagnostics, or changing the test's intended numeric result.
Run the targeted test and the complete canary suite. Keep the diff scoped here.
