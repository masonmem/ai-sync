# React behavior canary

Fix the double-increment behavior in `Counter.tsx` and keep the existing user
contract. Add or refine a regression test only if needed. Run the targeted test
and the complete canary suite. Keep the diff scoped here.
